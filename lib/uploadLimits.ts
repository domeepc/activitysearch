/** Max size per image upload (avatars and activity images). Convex upload URLs have no hard cap but a 2-minute POST timeout. */
export const MAX_IMAGE_UPLOAD_BYTES = 32 * 1024 * 1024;

/** Short label for UI copy (e.g. dialogs, form hints). */
export const MAX_IMAGE_UPLOAD_LABEL = "32MB";
